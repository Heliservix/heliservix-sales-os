# Packages

Shared packages will hold code that is reused by deployable applications.

The first expected package is `shared/`, containing domain types, validation contracts, constants, and cross-application utilities once application code begins.
