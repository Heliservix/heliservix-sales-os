# CRM Seed Artifacts

This directory contains the initial CRM workbook and schema from the original project.

- `CRM_HeliserviX_v1.xlsx`: spreadsheet used as the initial commercial operating artifact.
- `CRM_SCHEMA.json`: field contract used to align workbook tabs with future database import mapping.

These files should be treated as migration inputs and business references, not as the final production data store.
