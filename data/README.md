# Data

This directory stores seed artifacts and structured data references used to build the platform foundation.

Production tenant data should live in the application database and managed storage, not as committed repository files.
